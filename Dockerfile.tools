FROM python:3.10-slim

WORKDIR /app

COPY tools_server.py .
RUN pip install fastapi uvicorn pydantic --no-cache-dir

EXPOSE 8000

CMD ["uvicorn", "tools_server:app", "--host", "0.0.0.0", "--port", "8000"]
